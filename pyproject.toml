[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "gluing-matrix-validation"
version = "0.0.0"
description = "Gluing matrix SVD validation"
readme = "README.md"
requires-python = ">=3.8"
